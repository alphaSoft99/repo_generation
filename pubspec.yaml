name: repo_generation
description: A new Flutter project.
version: 0.0.2
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  source_gen:
  repo_annotation:
    git:
      url: https://github.com/SunnatilloShavkatov/repo_annotation.git
      ref: master

dev_dependencies:
  build_test:
  build_runner:
  flutter_test:
    sdk: flutter

